# Project Instructions

## API Compatibility Rule

Any parameter added, removed, or modified in an OpenAI-compatible or Anthropic-compatible request/response model must match the upstream spec in name, type, default, and semantics. Use `/check-api-compat` to verify compliance.
